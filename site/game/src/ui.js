define(function(require) {
  var Game = require('./src/game');

  $(document).ready(function() {
    $('#game-container').hide();
    $('#loading').hide();
    $('#splash').show();
  });

  return {
    init: function() {
      $('');
    }
  };  

});
